# ⚠ GENERATED FILE — DO NOT EDIT.
# Copied verbatim from remediation_supplement.py by scripts/vendor_engine.py.
# Edit the backend module instead; this copy is regenerated at build time.
"""Auto-generated supplementary remediation entries for new rules."""

REMEDIATIONS_SUPPLEMENT = {
    "CIS-AWS-AA-ARCH": {
        "summary": "Ensure Access Analyzer archive rules are configured",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Access Analyzer archive rules are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-AA-FIND": {
        "summary": "Ensure Access Analyzer findings are reviewed and resolved",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Access Analyzer findings are reviewed and resolved",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-AA-POL": {
        "summary": "Ensure Access Analyzer validates IAM policy changes",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Access Analyzer validates IAM policy changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-AA-PUB": {
        "summary": "Ensure Access Analyzer monitors public access",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Access Analyzer monitors public access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-AA-XACCT": {
        "summary": "Ensure Access Analyzer monitors cross-account access",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Access Analyzer monitors cross-account access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-ACM-EXP": {
        "summary": "Ensure ACM certificates are renewed before expiry",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure ACM certificates are renewed before expiry",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-CF-GEO": {
        "summary": "Ensure CloudFront uses geo-restriction when required",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure CloudFront uses geo-restriction when required",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-CF-OAI": {
        "summary": "Ensure CloudFront uses OAC/OAI for S3 origins",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure CloudFront uses OAC/OAI for S3 origins",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-CF-TLS": {
        "summary": "Ensure CloudFront uses TLSv1.2 minimum viewer protocol",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure CloudFront uses TLSv1.2 minimum viewer protocol",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-CFG-AGG": {
        "summary": "Ensure Config aggregator is configured for all regions",
        "steps": [
            "1. Navigate to the CONFIG console section",
            "2. Review the configuration as described in: Ensure Config aggregator is configured for all regions",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws configservice describe-configuration-recorders",
    },
    "CIS-AWS-CFG-PACK": {
        "summary": "Ensure Config conformance packs are deployed",
        "steps": [
            "1. Navigate to the CONFIG console section",
            "2. Review the configuration as described in: Ensure Config conformance packs are deployed",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws configservice describe-configuration-recorders",
    },
    "CIS-AWS-CFG-REM": {
        "summary": "Ensure Config remediation actions are configured",
        "steps": [
            "1. Navigate to the CONFIG console section",
            "2. Review the configuration as described in: Ensure Config remediation actions are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws configservice describe-configuration-recorders",
    },
    "CIS-AWS-CFG-RULES": {
        "summary": "Ensure Config rules are set for security best practices",
        "steps": [
            "1. Navigate to the CONFIG console section",
            "2. Review the configuration as described in: Ensure Config rules are set for security best practices",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws configservice describe-configuration-recorders",
    },
    "CIS-AWS-CFG-SNS": {
        "summary": "Ensure Config sends SNS notifications for changes",
        "steps": [
            "1. Navigate to the CONFIG console section",
            "2. Review the configuration as described in: Ensure Config sends SNS notifications for changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws configservice describe-configuration-recorders",
    },
    "CIS-AWS-CT-DATA": {
        "summary": "Ensure CloudTrail is logging data events for S3 and Lambda",
        "steps": [
            "1. Navigate to the CLOUDTRAIL console section",
            "2. Review the configuration as described in: Ensure CloudTrail is logging data events for S3 and Lambda",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws cloudtrail describe-trails --query \"trailList[].Name\"",
    },
    "CIS-AWS-CT-INSIGHT": {
        "summary": "Ensure CloudTrail Insights events are enabled",
        "steps": [
            "1. Navigate to the CLOUDTRAIL console section",
            "2. Review the configuration as described in: Ensure CloudTrail Insights events are enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws cloudtrail describe-trails --query \"trailList[].Name\"",
    },
    "CIS-AWS-CT-ORG": {
        "summary": "Ensure CloudTrail is configured as an Organization trail",
        "steps": [
            "1. Navigate to the CLOUDTRAIL console section",
            "2. Review the configuration as described in: Ensure CloudTrail is configured as an Organization trail",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws cloudtrail describe-trails --query \"trailList[].Name\"",
    },
    "CIS-AWS-CT-S3LOG": {
        "summary": "Ensure CloudTrail S3 bucket has access logging enabled",
        "steps": [
            "1. Navigate to the CLOUDTRAIL console section",
            "2. Review the configuration as described in: Ensure CloudTrail S3 bucket has access logging enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws cloudtrail describe-trails --query \"trailList[].Name\"",
    },
    "CIS-AWS-CW-ALARM-1": {
        "summary": "Ensure a metric filter and alarm exist for unauthorized API calls",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for unauthorized API calls",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-10": {
        "summary": "Ensure a metric filter and alarm exist for security group changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for security group changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-11": {
        "summary": "Ensure a metric filter and alarm exist for NACL changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for NACL changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-12": {
        "summary": "Ensure a metric filter and alarm exist for network gateway changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for network gateway changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-13": {
        "summary": "Ensure a metric filter and alarm exist for route table changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for route table changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-14": {
        "summary": "Ensure a metric filter and alarm exist for VPC changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for VPC changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-2": {
        "summary": "Ensure a metric filter and alarm exist for console sign-in without MFA",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for console sign-in without MFA",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-3": {
        "summary": "Ensure a metric filter and alarm exist for root account usage",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for root account usage",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-4": {
        "summary": "Ensure a metric filter and alarm exist for IAM policy changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for IAM policy changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-5": {
        "summary": "Ensure a metric filter and alarm exist for CloudTrail config changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for CloudTrail config changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-6": {
        "summary": "Ensure a metric filter and alarm exist for console auth failures",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for console auth failures",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-7": {
        "summary": "Ensure a metric filter and alarm exist for disabling CMK",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for disabling CMK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-8": {
        "summary": "Ensure a metric filter and alarm exist for S3 bucket policy changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for S3 bucket policy changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CW-ALARM-9": {
        "summary": "Ensure a metric filter and alarm exist for Config config changes",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure a metric filter and alarm exist for Config config changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-CWL-KMS": {
        "summary": "Ensure CloudWatch Log Groups are encrypted with KMS",
        "steps": [
            "1. Navigate to the CLOUDWATCH console section",
            "2. Review the configuration as described in: Ensure CloudWatch Log Groups are encrypted with KMS",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws logs describe-log-groups --query \"logGroups[].logGroupName\"",
    },
    "CIS-AWS-DDB-BACKUP": {
        "summary": "Ensure DynamoDB tables have backup configured",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure DynamoDB tables have backup configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-DDB-ENC": {
        "summary": "Ensure DynamoDB tables are encrypted with CMK",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure DynamoDB tables are encrypted with CMK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-DDB-PITR": {
        "summary": "Ensure DynamoDB point-in-time recovery is enabled",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure DynamoDB point-in-time recovery is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-EBS-ATT": {
        "summary": "Ensure unused EBS volumes are identified and removed",
        "steps": [
            "1. Navigate to the EBS console section",
            "2. Review the configuration as described in: Ensure unused EBS volumes are identified and removed",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-volumes --query \"Volumes[].[VolumeId,Encrypted]\"",
    },
    "CIS-AWS-EBS-DEF": {
        "summary": "Ensure EBS default encryption is enabled for the region",
        "steps": [
            "1. Navigate to the EBS console section",
            "2. Review the configuration as described in: Ensure EBS default encryption is enabled for the region",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-volumes --query \"Volumes[].[VolumeId,Encrypted]\"",
    },
    "CIS-AWS-EBS-IOPS": {
        "summary": "Ensure EBS volumes use appropriate volume types",
        "steps": [
            "1. Navigate to the EBS console section",
            "2. Review the configuration as described in: Ensure EBS volumes use appropriate volume types",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-volumes --query \"Volumes[].[VolumeId,Encrypted]\"",
    },
    "CIS-AWS-EBS-SNAP": {
        "summary": "Ensure EBS snapshots are not publicly shared",
        "steps": [
            "1. Navigate to the EBS console section",
            "2. Review the configuration as described in: Ensure EBS snapshots are not publicly shared",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-volumes --query \"Volumes[].[VolumeId,Encrypted]\"",
    },
    "CIS-AWS-EBS-TAG": {
        "summary": "Ensure EBS volumes are properly tagged",
        "steps": [
            "1. Navigate to the EBS console section",
            "2. Review the configuration as described in: Ensure EBS volumes are properly tagged",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-volumes --query \"Volumes[].[VolumeId,Encrypted]\"",
    },
    "CIS-AWS-EC2-AMI-1": {
        "summary": "Ensure EC2 AMIs are not publicly shared",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure EC2 AMIs are not publicly shared",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-EC2-EBS-OPT": {
        "summary": "Ensure EBS-optimized is enabled for supported instance types",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure EBS-optimized is enabled for supported instance types",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-EC2-IMDS": {
        "summary": "Ensure EC2 instances use IMDSv2",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure EC2 instances use IMDSv2",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-EC2-MON-1": {
        "summary": "Ensure detailed monitoring is enabled for EC2 instances",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure detailed monitoring is enabled for EC2 instances",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-EC2-SSM-1": {
        "summary": "Ensure EC2 instances are managed by AWS Systems Manager",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure EC2 instances are managed by AWS Systems Manager",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-EC2-TERM-1": {
        "summary": "Ensure termination protection is enabled for production instances",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure termination protection is enabled for production instances",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-ECR-IMMUT": {
        "summary": "Ensure ECR image tag immutability is enabled",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure ECR image tag immutability is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-ECR-LC": {
        "summary": "Ensure ECR lifecycle policies are configured",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure ECR lifecycle policies are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-ECR-SCAN": {
        "summary": "Ensure ECR image scanning is enabled",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure ECR image scanning is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-EKS-EP": {
        "summary": "Ensure EKS endpoint access is not publicly accessible",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS endpoint access is not publicly accessible",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-EKS-LOG": {
        "summary": "Ensure EKS cluster control plane logging is enabled",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS cluster control plane logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-EKS-NET": {
        "summary": "Ensure EKS pods use network policies",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS pods use network policies",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-EKS-PRIV": {
        "summary": "Ensure EKS cluster nodes use private subnets",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS cluster nodes use private subnets",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-EKS-PSP": {
        "summary": "Ensure EKS pod security standards are enforced",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS pod security standards are enforced",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-EKS-RBAC": {
        "summary": "Ensure EKS uses RBAC for authorization",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS uses RBAC for authorization",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-EKS-SEC": {
        "summary": "Ensure EKS secrets are encrypted with KMS CMK",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS secrets are encrypted with KMS CMK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-EKS-VER": {
        "summary": "Ensure EKS cluster is running a supported Kubernetes version",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS cluster is running a supported Kubernetes version",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-EKS-WI": {
        "summary": "Ensure EKS uses IRSA (IAM Roles for Service Accounts)",
        "steps": [
            "1. Navigate to the EKS console section",
            "2. Review the configuration as described in: Ensure EKS uses IRSA (IAM Roles for Service Accounts)",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws eks list-clusters",
    },
    "CIS-AWS-ELB-DEL": {
        "summary": "Ensure ALB has deletion protection enabled",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure ALB has deletion protection enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-ELB-DESYNCH": {
        "summary": "Ensure ALB desync mitigation mode is set to strictest",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure ALB desync mitigation mode is set to strictest",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-ELB-DROP": {
        "summary": "Ensure ALB drops invalid HTTP headers",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure ALB drops invalid HTTP headers",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-ELB-IDLE": {
        "summary": "Ensure ALB idle timeout is properly configured",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure ALB idle timeout is properly configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-ELB-TLS": {
        "summary": "Ensure ALB uses TLS 1.2 or higher security policy",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure ALB uses TLS 1.2 or higher security policy",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-ELB-WAF": {
        "summary": "Ensure ALB has WAF associated",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure ALB has WAF associated",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-GD-EKS": {
        "summary": "Ensure GuardDuty EKS Protection is enabled",
        "steps": [
            "1. Navigate to the GUARDDUTY console section",
            "2. Review the configuration as described in: Ensure GuardDuty EKS Protection is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws guardduty list-detectors",
    },
    "CIS-AWS-GD-FIND": {
        "summary": "Ensure GuardDuty findings are exported to S3",
        "steps": [
            "1. Navigate to the GUARDDUTY console section",
            "2. Review the configuration as described in: Ensure GuardDuty findings are exported to S3",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws guardduty list-detectors",
    },
    "CIS-AWS-GD-INTEL": {
        "summary": "Ensure GuardDuty threat intelligence lists are configured",
        "steps": [
            "1. Navigate to the GUARDDUTY console section",
            "2. Review the configuration as described in: Ensure GuardDuty threat intelligence lists are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws guardduty list-detectors",
    },
    "CIS-AWS-GD-MALWARE": {
        "summary": "Ensure GuardDuty Malware Protection is enabled",
        "steps": [
            "1. Navigate to the GUARDDUTY console section",
            "2. Review the configuration as described in: Ensure GuardDuty Malware Protection is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws guardduty list-detectors",
    },
    "CIS-AWS-GD-MEM": {
        "summary": "Ensure GuardDuty is enabled for all member accounts",
        "steps": [
            "1. Navigate to the GUARDDUTY console section",
            "2. Review the configuration as described in: Ensure GuardDuty is enabled for all member accounts",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws guardduty list-detectors",
    },
    "CIS-AWS-GD-S3": {
        "summary": "Ensure GuardDuty S3 Protection is enabled",
        "steps": [
            "1. Navigate to the GUARDDUTY console section",
            "2. Review the configuration as described in: Ensure GuardDuty S3 Protection is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws guardduty list-detectors",
    },
    "CIS-AWS-IAM-PWD-1": {
        "summary": "Ensure IAM password policy requires minimum 14 characters",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM password policy requires minimum 14 characters",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-PWD-2": {
        "summary": "Ensure IAM password policy requires at least one uppercase letter",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM password policy requires at least one uppercase letter",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-PWD-3": {
        "summary": "Ensure IAM password policy requires at least one lowercase letter",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM password policy requires at least one lowercase letter",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-PWD-4": {
        "summary": "Ensure IAM password policy requires at least one symbol",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM password policy requires at least one symbol",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-PWD-5": {
        "summary": "Ensure IAM password policy requires at least one number",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM password policy requires at least one number",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-PWD-6": {
        "summary": "Ensure IAM password policy prevents password reuse (24 generations)",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM password policy prevents password reuse (24 generations)",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-PWD-7": {
        "summary": "Ensure IAM password policy expires passwords within 90 days",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM password policy expires passwords within 90 days",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-ROLE-1": {
        "summary": "Ensure IAM policies are attached to groups or roles only",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM policies are attached to groups or roles only",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-ROLE-2": {
        "summary": "Ensure IAM users receive permissions only through groups",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM users receive permissions only through groups",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-ROLE-3": {
        "summary": "Ensure a support role has been created for AWS Support access",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure a support role has been created for AWS Support access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-ROLE-4": {
        "summary": "Ensure IAM instance roles are used for resource access from instances",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM instance roles are used for resource access from instances",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-ROLE-5": {
        "summary": "Ensure permissions boundary is set on all IAM roles",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure permissions boundary is set on all IAM roles",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-IAM-ROLE-6": {
        "summary": "Ensure cross-account IAM roles use external IDs",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure cross-account IAM roles use external IDs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws iam get-account-summary",
    },
    "CIS-AWS-KMS-ALIAS": {
        "summary": "Ensure KMS keys use aliases for management clarity",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS keys use aliases for management clarity",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws kms list-keys --query \"Keys[].KeyId\"",
    },
    "CIS-AWS-KMS-DEL": {
        "summary": "Ensure KMS key deletion waiting period is at least 7 days",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS key deletion waiting period is at least 7 days",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws kms list-keys --query \"Keys[].KeyId\"",
    },
    "CIS-AWS-KMS-GRANT": {
        "summary": "Ensure KMS key grants follow least privilege",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS key grants follow least privilege",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws kms list-keys --query \"Keys[].KeyId\"",
    },
    "CIS-AWS-KMS-POLICY": {
        "summary": "Ensure KMS key policies restrict key usage",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS key policies restrict key usage",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws kms list-keys --query \"Keys[].KeyId\"",
    },
    "CIS-AWS-KMS-XACCT": {
        "summary": "Ensure KMS keys are not shared cross-account without justification",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS keys are not shared cross-account without justification",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws kms list-keys --query \"Keys[].KeyId\"",
    },
    "CIS-AWS-LAM-CONC": {
        "summary": "Ensure Lambda reserved concurrency is configured",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure Lambda reserved concurrency is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-LAM-LAYER": {
        "summary": "Ensure Lambda layers are from trusted sources",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure Lambda layers are from trusted sources",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-LAM-LOG": {
        "summary": "Ensure Lambda functions have proper log group retention",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure Lambda functions have proper log group retention",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-LAM-MEM": {
        "summary": "Ensure Lambda memory and timeout are properly sized",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure Lambda memory and timeout are properly sized",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-LAM-SIGN": {
        "summary": "Ensure Lambda code signing is configured",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure Lambda code signing is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-LAM-VPC": {
        "summary": "Ensure Lambda functions run in a VPC when accessing private resources",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure Lambda functions run in a VPC when accessing private resources",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-LAM-XRAY": {
        "summary": "Ensure Lambda functions have X-Ray tracing enabled",
        "steps": [
            "1. Navigate to the LAMBDA console section",
            "2. Review the configuration as described in: Ensure Lambda functions have X-Ray tracing enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws lambda list-functions --query \"Functions[].FunctionName\"",
    },
    "CIS-AWS-MACIE-1": {
        "summary": "Ensure Amazon Macie is enabled for S3 data classification",
        "steps": [
            "1. Navigate to the S3 console section",
            "2. Review the configuration as described in: Ensure Amazon Macie is enabled for S3 data classification",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws s3api get-bucket-policy --bucket <bucket-name>",
    },
    "CIS-AWS-MACIE-2": {
        "summary": "Ensure Macie automated data discovery is enabled",
        "steps": [
            "1. Navigate to the S3 console section",
            "2. Review the configuration as described in: Ensure Macie automated data discovery is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws s3api get-bucket-policy --bucket <bucket-name>",
    },
    "CIS-AWS-ORG-BACKUP": {
        "summary": "Ensure AWS Backup is configured for critical resources",
        "steps": [
            "1. Navigate to the GENERAL console section",
            "2. Review the configuration as described in: Ensure AWS Backup is configured for critical resources",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws general describe-unknowns 2>/dev/null || echo \"Check via AWS Console\"",
    },
    "CIS-AWS-ORG-BILL": {
        "summary": "Ensure billing alerts and budgets are configured",
        "steps": [
            "1. Navigate to the GENERAL console section",
            "2. Review the configuration as described in: Ensure billing alerts and budgets are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws general describe-unknowns 2>/dev/null || echo \"Check via AWS Console\"",
    },
    "CIS-AWS-ORG-CT": {
        "summary": "Ensure AWS Control Tower is enabled for multi-account governance",
        "steps": [
            "1. Navigate to the GENERAL console section",
            "2. Review the configuration as described in: Ensure AWS Control Tower is enabled for multi-account governance",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws general describe-unknowns 2>/dev/null || echo \"Check via AWS Console\"",
    },
    "CIS-AWS-ORG-SCP": {
        "summary": "Ensure Service Control Policies restrict dangerous services",
        "steps": [
            "1. Navigate to the GENERAL console section",
            "2. Review the configuration as described in: Ensure Service Control Policies restrict dangerous services",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws general describe-unknowns 2>/dev/null || echo \"Check via AWS Console\"",
    },
    "CIS-AWS-ORG-SEC": {
        "summary": "Ensure Security Hub is enabled and collecting findings",
        "steps": [
            "1. Navigate to the GENERAL console section",
            "2. Review the configuration as described in: Ensure Security Hub is enabled and collecting findings",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws general describe-unknowns 2>/dev/null || echo \"Check via AWS Console\"",
    },
    "CIS-AWS-ORG-SSO": {
        "summary": "Ensure AWS IAM Identity Center (SSO) is used for multi-account access",
        "steps": [
            "1. Navigate to the GENERAL console section",
            "2. Review the configuration as described in: Ensure AWS IAM Identity Center (SSO) is used for multi-account access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws general describe-unknowns 2>/dev/null || echo \"Check via AWS Console\"",
    },
    "CIS-AWS-ORG-TAG": {
        "summary": "Ensure tag policies are enforced across the organization",
        "steps": [
            "1. Navigate to the GENERAL console section",
            "2. Review the configuration as described in: Ensure tag policies are enforced across the organization",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws general describe-unknowns 2>/dev/null || echo \"Check via AWS Console\"",
    },
    "CIS-AWS-R53-DNSSEC": {
        "summary": "Ensure Route 53 hosted zones use DNSSEC signing",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure Route 53 hosted zones use DNSSEC signing",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-RDS-COPY": {
        "summary": "Ensure RDS snapshots are not public",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure RDS snapshots are not public",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-RDS-IAM": {
        "summary": "Ensure RDS instances have IAM Database Authentication enabled",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure RDS instances have IAM Database Authentication enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-RDS-LOG": {
        "summary": "Ensure RDS instances have enhanced monitoring enabled",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure RDS instances have enhanced monitoring enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-RDS-PATCH": {
        "summary": "Ensure RDS auto minor version upgrade is enabled",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure RDS auto minor version upgrade is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-RDS-PI": {
        "summary": "Ensure RDS Performance Insights is enabled",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure RDS Performance Insights is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-RDS-SSL": {
        "summary": "Ensure RDS connections use SSL/TLS",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure RDS connections use SSL/TLS",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-RDS-TAG": {
        "summary": "Ensure RDS instances are properly tagged",
        "steps": [
            "1. Navigate to the RDS console section",
            "2. Review the configuration as described in: Ensure RDS instances are properly tagged",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws rds describe-db-instances --query \"DBInstances[].[DBInstanceIdentifier,Engine]\"",
    },
    "CIS-AWS-S3-CORS-1": {
        "summary": "Ensure S3 buckets do not have overly permissive CORS",
        "steps": [
            "1. Navigate to the S3 console section",
            "2. Review the configuration as described in: Ensure S3 buckets do not have overly permissive CORS",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws s3api get-bucket-policy --bucket <bucket-name>",
    },
    "CIS-AWS-S3-LC-1": {
        "summary": "Ensure S3 bucket lifecycle rules are configured",
        "steps": [
            "1. Navigate to the S3 console section",
            "2. Review the configuration as described in: Ensure S3 bucket lifecycle rules are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws s3api get-bucket-policy --bucket <bucket-name>",
    },
    "CIS-AWS-S3-LOG-1": {
        "summary": "Ensure S3 bucket access logging is enabled",
        "steps": [
            "1. Navigate to the S3 console section",
            "2. Review the configuration as described in: Ensure S3 bucket access logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws s3api get-bucket-policy --bucket <bucket-name>",
    },
    "CIS-AWS-S3-MFA-1": {
        "summary": "Ensure MFA Delete is enabled on S3 buckets",
        "steps": [
            "1. Navigate to the S3 console section",
            "2. Review the configuration as described in: Ensure MFA Delete is enabled on S3 buckets",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws s3api get-bucket-policy --bucket <bucket-name>",
    },
    "CIS-AWS-S3-OBJ-1": {
        "summary": "Ensure S3 Object Lock is enabled for compliance-critical buckets",
        "steps": [
            "1. Navigate to the S3 console section",
            "2. Review the configuration as described in: Ensure S3 Object Lock is enabled for compliance-critical buckets",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws s3api get-bucket-policy --bucket <bucket-name>",
    },
    "CIS-AWS-S3-REP-1": {
        "summary": "Ensure S3 cross-region replication is configured for critical buckets",
        "steps": [
            "1. Navigate to the S3 console section",
            "2. Review the configuration as described in: Ensure S3 cross-region replication is configured for critical buckets",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws s3api get-bucket-policy --bucket <bucket-name>",
    },
    "CIS-AWS-SHIELD-1": {
        "summary": "Ensure AWS Shield Advanced is enabled for DDoS protection",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure AWS Shield Advanced is enabled for DDoS protection",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-SM-ACC": {
        "summary": "Ensure Secrets Manager resource policies restrict access",
        "steps": [
            "1. Navigate to the SECRETSMANAGER console section",
            "2. Review the configuration as described in: Ensure Secrets Manager resource policies restrict access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws secretsmanager list-secrets --query \"SecretList[].Name\"",
    },
    "CIS-AWS-SM-KMS": {
        "summary": "Ensure Secrets Manager secrets are encrypted with CMK",
        "steps": [
            "1. Navigate to the SECRETSMANAGER console section",
            "2. Review the configuration as described in: Ensure Secrets Manager secrets are encrypted with CMK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws secretsmanager list-secrets --query \"SecretList[].Name\"",
    },
    "CIS-AWS-SM-ROT": {
        "summary": "Ensure Secrets Manager secrets have rotation enabled",
        "steps": [
            "1. Navigate to the SECRETSMANAGER console section",
            "2. Review the configuration as described in: Ensure Secrets Manager secrets have rotation enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws secretsmanager list-secrets --query \"SecretList[].Name\"",
    },
    "CIS-AWS-SM-TAG": {
        "summary": "Ensure Secrets Manager secrets are properly tagged",
        "steps": [
            "1. Navigate to the SECRETSMANAGER console section",
            "2. Review the configuration as described in: Ensure Secrets Manager secrets are properly tagged",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws secretsmanager list-secrets --query \"SecretList[].Name\"",
    },
    "CIS-AWS-SNS-DLQ": {
        "summary": "Ensure SNS subscriptions have DLQ configured",
        "steps": [
            "1. Navigate to the SNS console section",
            "2. Review the configuration as described in: Ensure SNS subscriptions have DLQ configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws sns list-topics",
    },
    "CIS-AWS-SNS-ENC": {
        "summary": "Ensure SNS topics are encrypted at rest",
        "steps": [
            "1. Navigate to the SNS console section",
            "2. Review the configuration as described in: Ensure SNS topics are encrypted at rest",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws sns list-topics",
    },
    "CIS-AWS-SNS-POL": {
        "summary": "Ensure SNS topic policies do not allow public access",
        "steps": [
            "1. Navigate to the SNS console section",
            "2. Review the configuration as described in: Ensure SNS topic policies do not allow public access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws sns list-topics",
    },
    "CIS-AWS-SNS-SUB": {
        "summary": "Ensure SNS subscriptions require confirmation",
        "steps": [
            "1. Navigate to the SNS console section",
            "2. Review the configuration as described in: Ensure SNS subscriptions require confirmation",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws sns list-topics",
    },
    "CIS-AWS-SQS-DLQ": {
        "summary": "Ensure SQS queues have dead-letter queues configured",
        "steps": [
            "1. Navigate to the SQS console section",
            "2. Review the configuration as described in: Ensure SQS queues have dead-letter queues configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws sqs list-queues",
    },
    "CIS-AWS-SQS-ENC": {
        "summary": "Ensure SQS queues are encrypted at rest",
        "steps": [
            "1. Navigate to the SQS console section",
            "2. Review the configuration as described in: Ensure SQS queues are encrypted at rest",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws sqs list-queues",
    },
    "CIS-AWS-SQS-POL": {
        "summary": "Ensure SQS queue policies do not allow public access",
        "steps": [
            "1. Navigate to the SQS console section",
            "2. Review the configuration as described in: Ensure SQS queue policies do not allow public access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws sqs list-queues",
    },
    "CIS-AWS-SQS-VIS": {
        "summary": "Ensure SQS visibility timeout is appropriately configured",
        "steps": [
            "1. Navigate to the SQS console section",
            "2. Review the configuration as described in: Ensure SQS visibility timeout is appropriately configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws sqs list-queues",
    },
    "CIS-AWS-SSM-PARAM": {
        "summary": "Ensure SSM Parameter Store secrets use encryption",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure SSM Parameter Store secrets use encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-SSM-PATCH": {
        "summary": "Ensure Systems Manager Patch Manager is configured",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure Systems Manager Patch Manager is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-VPC-EP-1": {
        "summary": "Ensure VPC endpoints are used for S3 and DynamoDB access",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure VPC endpoints are used for S3 and DynamoDB access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-VPC-FLOW": {
        "summary": "Ensure VPC Flow Logging is enabled in all VPCs",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure VPC Flow Logging is enabled in all VPCs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-VPC-NACL-1": {
        "summary": "Ensure Network ACLs do not allow unrestricted egress",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure Network ACLs do not allow unrestricted egress",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-VPC-NAT-1": {
        "summary": "Ensure NAT Gateways are used instead of NAT instances",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure NAT Gateways are used instead of NAT instances",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-VPC-PEER-1": {
        "summary": "Ensure VPC peering connections follow least privilege",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure VPC peering connections follow least privilege",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-VPC-SG-DESC": {
        "summary": "Ensure all security groups have descriptions",
        "steps": [
            "1. Navigate to the EC2 console section",
            "2. Review the configuration as described in: Ensure all security groups have descriptions",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws ec2 describe-instances --query \"Reservations[].Instances[].[InstanceId,State.Name]\"",
    },
    "CIS-AWS-WAFV2-1": {
        "summary": "Ensure WAFv2 web ACLs are associated with ALBs and API Gateways",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure WAFv2 web ACLs are associated with ALBs and API Gateways",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-AWS-WAFV2-2": {
        "summary": "Ensure WAFv2 logging is enabled",
        "steps": [
            "1. Navigate to the ELB console section",
            "2. Review the configuration as described in: Ensure WAFv2 logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/amazon_web_services",
        "cli_example": "aws elbv2 describe-load-balancers --query \"LoadBalancers[].LoadBalancerArn\"",
    },
    "CIS-Azure-1.1": {
        "summary": "Ensure Security Defaults or Conditional Access policies are configured",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure Security Defaults or Conditional Access policies are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.10": {
        "summary": "Ensure Privileged Identity Management is used",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure Privileged Identity Management is used",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.11": {
        "summary": "Ensure Security Defaults or Conditional Access blocks sign-in risk",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure Security Defaults or Conditional Access blocks sign-in risk",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.3": {
        "summary": "Ensure guest users are reviewed on a regular basis",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure guest users are reviewed on a regular basis",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.4": {
        "summary": "Ensure no custom subscription administrator roles exist",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure no custom subscription administrator roles exist",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.5": {
        "summary": "Ensure MFA is enabled for all users",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure MFA is enabled for all users",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.6": {
        "summary": "Ensure Conditional Access requires MFA for administrators",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure Conditional Access requires MFA for administrators",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.7": {
        "summary": "Ensure Conditional Access requires MFA for Azure Management",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure Conditional Access requires MFA for Azure Management",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.8": {
        "summary": "Ensure user consent to applications is restricted",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure user consent to applications is restricted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-1.9": {
        "summary": "Ensure legacy authentication is blocked",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure legacy authentication is blocked",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-2.2": {
        "summary": "Ensure auto provisioning of monitoring agent is enabled",
        "steps": [
            "1. Navigate to the SECURITY_CENTER console section",
            "2. Review the configuration as described in: Ensure auto provisioning of monitoring agent is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az security pricing list --query \"[].{Name:name,Tier:pricingTier}\"",
    },
    "CIS-Azure-2.3": {
        "summary": "Ensure Defender for Servers is enabled",
        "steps": [
            "1. Navigate to the SECURITY_CENTER console section",
            "2. Review the configuration as described in: Ensure Defender for Servers is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az security pricing list --query \"[].{Name:name,Tier:pricingTier}\"",
    },
    "CIS-Azure-2.4": {
        "summary": "Ensure Defender for App Service is enabled",
        "steps": [
            "1. Navigate to the SECURITY_CENTER console section",
            "2. Review the configuration as described in: Ensure Defender for App Service is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az security pricing list --query \"[].{Name:name,Tier:pricingTier}\"",
    },
    "CIS-Azure-2.5": {
        "summary": "Ensure Defender for SQL is enabled",
        "steps": [
            "1. Navigate to the SECURITY_CENTER console section",
            "2. Review the configuration as described in: Ensure Defender for SQL is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az security pricing list --query \"[].{Name:name,Tier:pricingTier}\"",
    },
    "CIS-Azure-2.6": {
        "summary": "Ensure Defender for Storage is enabled",
        "steps": [
            "1. Navigate to the SECURITY_CENTER console section",
            "2. Review the configuration as described in: Ensure Defender for Storage is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az security pricing list --query \"[].{Name:name,Tier:pricingTier}\"",
    },
    "CIS-Azure-2.7": {
        "summary": "Ensure Defender for Containers is enabled",
        "steps": [
            "1. Navigate to the SECURITY_CENTER console section",
            "2. Review the configuration as described in: Ensure Defender for Containers is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az security pricing list --query \"[].{Name:name,Tier:pricingTier}\"",
    },
    "CIS-Azure-2.8": {
        "summary": "Ensure Defender for Key Vault is enabled",
        "steps": [
            "1. Navigate to the SECURITY_CENTER console section",
            "2. Review the configuration as described in: Ensure Defender for Key Vault is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az security pricing list --query \"[].{Name:name,Tier:pricingTier}\"",
    },
    "CIS-Azure-2.9": {
        "summary": "Ensure Defender for DNS is enabled",
        "steps": [
            "1. Navigate to the SECURITY_CENTER console section",
            "2. Review the configuration as described in: Ensure Defender for DNS is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az security pricing list --query \"[].{Name:name,Tier:pricingTier}\"",
    },
    "CIS-Azure-3.10": {
        "summary": "Ensure cross-tenant replication is not allowed",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure cross-tenant replication is not allowed",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az storage account list --query \"[].{Name:name,Encryption:encryption.services}\"",
    },
    "CIS-Azure-3.2": {
        "summary": "Ensure storage account infrastructure encryption is enabled",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure storage account infrastructure encryption is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az storage account list --query \"[].{Name:name,Encryption:encryption.services}\"",
    },
    "CIS-Azure-3.3": {
        "summary": "Ensure storage logging is enabled for Blob service",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure storage logging is enabled for Blob service",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az storage account list --query \"[].{Name:name,Encryption:encryption.services}\"",
    },
    "CIS-Azure-3.4": {
        "summary": "Ensure storage logging is enabled for Table service",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure storage logging is enabled for Table service",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az storage account list --query \"[].{Name:name,Encryption:encryption.services}\"",
    },
    "CIS-Azure-3.5": {
        "summary": "Ensure storage logging is enabled for Queue service",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure storage logging is enabled for Queue service",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az storage account list --query \"[].{Name:name,Encryption:encryption.services}\"",
    },
    "CIS-Azure-3.6": {
        "summary": "Ensure soft delete is enabled for Azure blobs",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure soft delete is enabled for Azure blobs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az storage account list --query \"[].{Name:name,Encryption:encryption.services}\"",
    },
    "CIS-Azure-3.8": {
        "summary": "Ensure storage accounts use private endpoints",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure storage accounts use private endpoints",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az storage account list --query \"[].{Name:name,Encryption:encryption.services}\"",
    },
    "CIS-Azure-3.9": {
        "summary": "Ensure storage account public network access is disabled",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure storage account public network access is disabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az storage account list --query \"[].{Name:name,Encryption:encryption.services}\"",
    },
    "CIS-Azure-4.10": {
        "summary": "Ensure Azure SQL Database connection policy is set to Redirect",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Azure SQL Database connection policy is set to Redirect",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-4.11": {
        "summary": "Ensure SQL databases have long-term backup retention",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL databases have long-term backup retention",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-4.3": {
        "summary": "Ensure SQL auditing retention is >= 90 days",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL auditing retention is >= 90 days",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-4.5": {
        "summary": "Ensure SQL server vulnerability assessment is enabled",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL server vulnerability assessment is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-4.7": {
        "summary": "Ensure SQL server firewall does not use Allow Azure Services rule",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL server firewall does not use Allow Azure Services rule",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-4.9": {
        "summary": "Ensure SQL server AAD admin is configured",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL server AAD admin is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-5.1": {
        "summary": "Ensure diagnostic settings capture admin activity",
        "steps": [
            "1. Navigate to the MONITOR console section",
            "2. Review the configuration as described in: Ensure diagnostic settings capture admin activity",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor diagnostic-settings list --resource <resource-id>",
    },
    "CIS-Azure-5.3": {
        "summary": "Ensure diagnostic setting captures all categories",
        "steps": [
            "1. Navigate to the MONITOR console section",
            "2. Review the configuration as described in: Ensure diagnostic setting captures all categories",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor diagnostic-settings list --resource <resource-id>",
    },
    "CIS-Azure-5.4": {
        "summary": "Ensure Activity Log alerts exist for Create Policy Assignment",
        "steps": [
            "1. Navigate to the MONITOR console section",
            "2. Review the configuration as described in: Ensure Activity Log alerts exist for Create Policy Assignment",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor diagnostic-settings list --resource <resource-id>",
    },
    "CIS-Azure-5.5": {
        "summary": "Ensure Activity Log alerts exist for Create/Update NSG",
        "steps": [
            "1. Navigate to the MONITOR console section",
            "2. Review the configuration as described in: Ensure Activity Log alerts exist for Create/Update NSG",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor diagnostic-settings list --resource <resource-id>",
    },
    "CIS-Azure-5.6": {
        "summary": "Ensure Activity Log alerts exist for Delete NSG",
        "steps": [
            "1. Navigate to the MONITOR console section",
            "2. Review the configuration as described in: Ensure Activity Log alerts exist for Delete NSG",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor diagnostic-settings list --resource <resource-id>",
    },
    "CIS-Azure-5.7": {
        "summary": "Ensure Activity Log alerts exist for Create/Update Security Solution",
        "steps": [
            "1. Navigate to the MONITOR console section",
            "2. Review the configuration as described in: Ensure Activity Log alerts exist for Create/Update Security Solution",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor diagnostic-settings list --resource <resource-id>",
    },
    "CIS-Azure-5.8": {
        "summary": "Ensure Activity Log alerts exist for Create/Update SQL Firewall",
        "steps": [
            "1. Navigate to the MONITOR console section",
            "2. Review the configuration as described in: Ensure Activity Log alerts exist for Create/Update SQL Firewall",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor diagnostic-settings list --resource <resource-id>",
    },
    "CIS-Azure-6.10": {
        "summary": "Ensure network interfaces do not have public IPs",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure network interfaces do not have public IPs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-6.4": {
        "summary": "Ensure Network Security Group Flow Log retention is > 90 days",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure Network Security Group Flow Log retention is > 90 days",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-6.6": {
        "summary": "Ensure DDoS Protection Standard is enabled",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure DDoS Protection Standard is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-6.8": {
        "summary": "Ensure Private DNS zones are used",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure Private DNS zones are used",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-6.9": {
        "summary": "Ensure service endpoints are configured for subnets",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure service endpoints are configured for subnets",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-7.10": {
        "summary": "Ensure VMs have JIT access configured",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VMs have JIT access configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.11": {
        "summary": "Ensure Trusted Launch is enabled for VMs",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Trusted Launch is enabled for VMs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.2": {
        "summary": "Ensure virtual machines have no unattached disks",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure virtual machines have no unattached disks",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.3": {
        "summary": "Ensure VM boot diagnostics are enabled",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VM boot diagnostics are enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.4": {
        "summary": "Ensure VMs use managed disks",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VMs use managed disks",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.5": {
        "summary": "Ensure VMs have endpoint protection installed",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VMs have endpoint protection installed",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.6": {
        "summary": "Ensure VM extensions are properly managed",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VM extensions are properly managed",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.7": {
        "summary": "Ensure OS disk encryption is enabled",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure OS disk encryption is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.8": {
        "summary": "Ensure data disk encryption is enabled",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure data disk encryption is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-7.9": {
        "summary": "Ensure auto OS updates are enabled",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure auto OS updates are enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-8.3": {
        "summary": "Ensure Key Vault keys have an expiration date",
        "steps": [
            "1. Navigate to the KEYVAULT console section",
            "2. Review the configuration as described in: Ensure Key Vault keys have an expiration date",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az keyvault list --query \"[].{Name:name,Sku:properties.sku.name}\"",
    },
    "CIS-Azure-8.4": {
        "summary": "Ensure Key Vault certificates have an expiration date",
        "steps": [
            "1. Navigate to the KEYVAULT console section",
            "2. Review the configuration as described in: Ensure Key Vault certificates have an expiration date",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az keyvault list --query \"[].{Name:name,Sku:properties.sku.name}\"",
    },
    "CIS-Azure-8.5": {
        "summary": "Ensure Key Vault RBAC authorization is used instead of access policies",
        "steps": [
            "1. Navigate to the KEYVAULT console section",
            "2. Review the configuration as described in: Ensure Key Vault RBAC authorization is used instead of access policies",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az keyvault list --query \"[].{Name:name,Sku:properties.sku.name}\"",
    },
    "CIS-Azure-8.6": {
        "summary": "Ensure Key Vault uses private endpoints",
        "steps": [
            "1. Navigate to the KEYVAULT console section",
            "2. Review the configuration as described in: Ensure Key Vault uses private endpoints",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az keyvault list --query \"[].{Name:name,Sku:properties.sku.name}\"",
    },
    "CIS-Azure-8.7": {
        "summary": "Ensure Key Vault firewall restricts network access",
        "steps": [
            "1. Navigate to the KEYVAULT console section",
            "2. Review the configuration as described in: Ensure Key Vault firewall restricts network access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az keyvault list --query \"[].{Name:name,Sku:properties.sku.name}\"",
    },
    "CIS-Azure-8.8": {
        "summary": "Ensure Key Vault diagnostic logging is enabled",
        "steps": [
            "1. Navigate to the KEYVAULT console section",
            "2. Review the configuration as described in: Ensure Key Vault diagnostic logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az keyvault list --query \"[].{Name:name,Sku:properties.sku.name}\"",
    },
    "CIS-Azure-8.9": {
        "summary": "Ensure Key Vault keys use RSA 2048-bit or higher",
        "steps": [
            "1. Navigate to the KEYVAULT console section",
            "2. Review the configuration as described in: Ensure Key Vault keys use RSA 2048-bit or higher",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az keyvault list --query \"[].{Name:name,Sku:properties.sku.name}\"",
    },
    "CIS-Azure-9.10": {
        "summary": "Ensure FTP is disabled for App Service",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure FTP is disabled for App Service",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-9.11": {
        "summary": "Ensure App Service logging is enabled",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure App Service logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-9.12": {
        "summary": "Ensure App Service has Always On enabled",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure App Service has Always On enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-9.4": {
        "summary": "Ensure App Service uses a managed identity",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure App Service uses a managed identity",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-9.5": {
        "summary": "Ensure App Service has CORS configured properly",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure App Service has CORS configured properly",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-9.6": {
        "summary": "Ensure App Service authentication is configured",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure App Service authentication is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-9.7": {
        "summary": "Ensure App Service uses latest supported language version",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure App Service uses latest supported language version",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-9.8": {
        "summary": "Ensure App Service uses VNet integration",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure App Service uses VNet integration",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-9.9": {
        "summary": "Ensure App Service has remote debugging turned off",
        "steps": [
            "1. Navigate to the APPSERVICE console section",
            "2. Review the configuration as described in: Ensure App Service has remote debugging turned off",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az webapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-ACR-10": {
        "summary": "Ensure ACR uses Premium SKU for security features",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR uses Premium SKU for security features",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-ACR-11": {
        "summary": "Ensure ACR export policy is disabled",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR export policy is disabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-ACR-3": {
        "summary": "Ensure ACR uses content trust",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR uses content trust",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-ACR-4": {
        "summary": "Ensure ACR images are scanned for vulnerabilities",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR images are scanned for vulnerabilities",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-ACR-5": {
        "summary": "Ensure ACR quarantine policy is enabled",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR quarantine policy is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-ACR-6": {
        "summary": "Ensure ACR uses geo-replication for DR",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR uses geo-replication for DR",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-ACR-7": {
        "summary": "Ensure ACR retention policy is configured",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR retention policy is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-ACR-8": {
        "summary": "Ensure ACR uses managed identity for pulls",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR uses managed identity for pulls",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-ACR-9": {
        "summary": "Ensure ACR webhook notifications are configured",
        "steps": [
            "1. Navigate to the CONTAINER console section",
            "2. Review the configuration as described in: Ensure ACR webhook notifications are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az acr list --query \"[].{Name:name,Sku:sku.name}\"",
    },
    "CIS-Azure-BACKUP-1": {
        "summary": "Ensure Azure Backup is configured for VMs",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Azure Backup is configured for VMs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-BACKUP-2": {
        "summary": "Ensure backup vaults use CMK encryption",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure backup vaults use CMK encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-BACKUP-3": {
        "summary": "Ensure backup soft delete is enabled",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure backup soft delete is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-DB-GEN-1": {
        "summary": "Ensure database servers use TLS 1.2 minimum",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database servers use TLS 1.2 minimum",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-10": {
        "summary": "Ensure database maintenance window is configured",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database maintenance window is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-2": {
        "summary": "Ensure database servers use private endpoints",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database servers use private endpoints",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-3": {
        "summary": "Ensure database servers have backup retention >= 7 days",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database servers have backup retention >= 7 days",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-4": {
        "summary": "Ensure database servers have geo-redundant backup",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database servers have geo-redundant backup",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-5": {
        "summary": "Ensure database diagnostic logging is enabled",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database diagnostic logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-6": {
        "summary": "Ensure database threat detection is configured",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database threat detection is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-7": {
        "summary": "Ensure database vulnerability assessment is enabled",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database vulnerability assessment is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-8": {
        "summary": "Ensure database allows only Azure AD authentication",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database allows only Azure AD authentication",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DB-GEN-9": {
        "summary": "Ensure database deny public network access",
        "steps": [
            "1. Navigate to the DB console section",
            "2. Review the configuration as described in: Ensure database deny public network access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-DISK-2": {
        "summary": "Ensure managed disks use server-side encryption with PMK",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure managed disks use server-side encryption with PMK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-DISK-3": {
        "summary": "Ensure unattached disks are encrypted",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure unattached disks are encrypted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-DISK-4": {
        "summary": "Ensure disk encryption sets use CMK",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure disk encryption sets use CMK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-DISK-5": {
        "summary": "Ensure temporary disks are encrypted",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure temporary disks are encrypted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-DISK-6": {
        "summary": "Ensure disk access is through private endpoints",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure disk access is through private endpoints",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-DISK-7": {
        "summary": "Ensure disk network access policy is configured",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure disk network access policy is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-DISK-8": {
        "summary": "Ensure confidential disk encryption is used where required",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure confidential disk encryption is used where required",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-DISK-9": {
        "summary": "Ensure OS disk types match workload requirements",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure OS disk types match workload requirements",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az vm list --query \"[].{Name:name,OS:storageProfile.osDisk.osType}\"",
    },
    "CIS-Azure-FUNC-10": {
        "summary": "Ensure Azure Functions diagnostics are enabled",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions diagnostics are enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-FUNC-2": {
        "summary": "Ensure Azure Functions use managed identity",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions use managed identity",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-FUNC-3": {
        "summary": "Ensure Azure Functions minimum TLS is 1.2",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions minimum TLS is 1.2",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-FUNC-4": {
        "summary": "Ensure Azure Functions authentication is enabled",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions authentication is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-FUNC-5": {
        "summary": "Ensure Azure Functions remote debugging is disabled",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions remote debugging is disabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-FUNC-6": {
        "summary": "Ensure Azure Functions use latest supported runtime",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions use latest supported runtime",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-FUNC-7": {
        "summary": "Ensure Azure Functions FTP is disabled",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions FTP is disabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-FUNC-8": {
        "summary": "Ensure Azure Functions have CORS properly configured",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions have CORS properly configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-FUNC-9": {
        "summary": "Ensure Azure Functions have VNet integration",
        "steps": [
            "1. Navigate to the FUNCTION console section",
            "2. Review the configuration as described in: Ensure Azure Functions have VNet integration",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az functionapp list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-GOV-1": {
        "summary": "Ensure Azure Policy is used to enforce organizational standards",
        "steps": [
            "1. Navigate to the GOVERNANCE console section",
            "2. Review the configuration as described in: Ensure Azure Policy is used to enforce organizational standards",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az policy assignment list --query \"[].{Name:name,Scope:scope}\"",
    },
    "CIS-Azure-GOV-2": {
        "summary": "Ensure resource locks are applied to critical resources",
        "steps": [
            "1. Navigate to the GOVERNANCE console section",
            "2. Review the configuration as described in: Ensure resource locks are applied to critical resources",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az policy assignment list --query \"[].{Name:name,Scope:scope}\"",
    },
    "CIS-Azure-GOV-3": {
        "summary": "Ensure Management Groups are used for subscription organization",
        "steps": [
            "1. Navigate to the GOVERNANCE console section",
            "2. Review the configuration as described in: Ensure Management Groups are used for subscription organization",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az policy assignment list --query \"[].{Name:name,Scope:scope}\"",
    },
    "CIS-Azure-GOV-4": {
        "summary": "Ensure Azure Blueprints are used for environment standardization",
        "steps": [
            "1. Navigate to the GOVERNANCE console section",
            "2. Review the configuration as described in: Ensure Azure Blueprints are used for environment standardization",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az policy assignment list --query \"[].{Name:name,Scope:scope}\"",
    },
    "CIS-Azure-GOV-5": {
        "summary": "Ensure cost management alerts and budgets are configured",
        "steps": [
            "1. Navigate to the GOVERNANCE console section",
            "2. Review the configuration as described in: Ensure cost management alerts and budgets are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az policy assignment list --query \"[].{Name:name,Scope:scope}\"",
    },
    "CIS-Azure-IDENTITY-1": {
        "summary": "Ensure named locations are defined for Conditional Access",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure named locations are defined for Conditional Access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-IDENTITY-2": {
        "summary": "Ensure break-glass emergency access accounts exist",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure break-glass emergency access accounts exist",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-IDENTITY-3": {
        "summary": "Ensure admin accounts are cloud-only",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure admin accounts are cloud-only",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-IDENTITY-4": {
        "summary": "Ensure self-service password reset is configured",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure self-service password reset is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-IDENTITY-5": {
        "summary": "Ensure password hash sync is enabled",
        "steps": [
            "1. Navigate to the AAD console section",
            "2. Review the configuration as described in: Ensure password hash sync is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az ad user list --query \"[].{Name:displayName,UPN:userPrincipalName}\"",
    },
    "CIS-Azure-LA-10": {
        "summary": "Ensure Log Analytics has archive policy set",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics has archive policy set",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-LA-2": {
        "summary": "Ensure Log Analytics uses CMK encryption",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics uses CMK encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-LA-3": {
        "summary": "Ensure Log Analytics has network access restricted",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics has network access restricted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-LA-4": {
        "summary": "Ensure Log Analytics data export is configured",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics data export is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-LA-5": {
        "summary": "Ensure Log Analytics linked storage uses CMK",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics linked storage uses CMK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-LA-6": {
        "summary": "Ensure Log Analytics workspace has RBAC configured",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics workspace has RBAC configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-LA-7": {
        "summary": "Ensure Log Analytics uses private link",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics uses private link",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-LA-8": {
        "summary": "Ensure Log Analytics daily cap alerts are configured",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics daily cap alerts are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-LA-9": {
        "summary": "Ensure Log Analytics ingestion access mode is set",
        "steps": [
            "1. Navigate to the MONITORING console section",
            "2. Review the configuration as described in: Ensure Log Analytics ingestion access mode is set",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az monitor log-analytics workspace list --query \"[].{Name:name}\"",
    },
    "CIS-Azure-MySQL-2": {
        "summary": "Ensure MySQL server uses private endpoint",
        "steps": [
            "1. Navigate to the MYSQL console section",
            "2. Review the configuration as described in: Ensure MySQL server uses private endpoint",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az mysql server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-MySQL-3": {
        "summary": "Ensure MySQL server uses latest supported version",
        "steps": [
            "1. Navigate to the MYSQL console section",
            "2. Review the configuration as described in: Ensure MySQL server uses latest supported version",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az mysql server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-MySQL-4": {
        "summary": "Ensure MySQL audit log is enabled",
        "steps": [
            "1. Navigate to the MYSQL console section",
            "2. Review the configuration as described in: Ensure MySQL audit log is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az mysql server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-MySQL-5": {
        "summary": "Ensure MySQL firewall does not allow 0.0.0.0/0",
        "steps": [
            "1. Navigate to the MYSQL console section",
            "2. Review the configuration as described in: Ensure MySQL firewall does not allow 0.0.0.0/0",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az mysql server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-MySQL-6": {
        "summary": "Ensure MySQL TLS version is 1.2 or higher",
        "steps": [
            "1. Navigate to the MYSQL console section",
            "2. Review the configuration as described in: Ensure MySQL TLS version is 1.2 or higher",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az mysql server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-MySQL-7": {
        "summary": "Ensure MySQL backup retention is adequate",
        "steps": [
            "1. Navigate to the MYSQL console section",
            "2. Review the configuration as described in: Ensure MySQL backup retention is adequate",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az mysql server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-MySQL-8": {
        "summary": "Ensure MySQL uses CMK encryption",
        "steps": [
            "1. Navigate to the MYSQL console section",
            "2. Review the configuration as described in: Ensure MySQL uses CMK encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az mysql server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-MySQL-9": {
        "summary": "Ensure MySQL slow query log is enabled",
        "steps": [
            "1. Navigate to the MYSQL console section",
            "2. Review the configuration as described in: Ensure MySQL slow query log is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az mysql server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-NW-10": {
        "summary": "Ensure ExpressRoute circuits are encrypted",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure ExpressRoute circuits are encrypted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-NW-2": {
        "summary": "Ensure NSG flow logs use Traffic Analytics",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure NSG flow logs use Traffic Analytics",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-NW-3": {
        "summary": "Ensure Network Watcher Connection Monitor is configured",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure Network Watcher Connection Monitor is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-NW-4": {
        "summary": "Ensure NSG rules are documented and reviewed",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure NSG rules are documented and reviewed",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-NW-5": {
        "summary": "Ensure application security groups are used",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure application security groups are used",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-NW-6": {
        "summary": "Ensure IP forwarding is disabled on NICs",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure IP forwarding is disabled on NICs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-NW-7": {
        "summary": "Ensure accelerated networking is enabled for supported VMs",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure accelerated networking is enabled for supported VMs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-NW-8": {
        "summary": "Ensure WAF is enabled on Application Gateway",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure WAF is enabled on Application Gateway",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-NW-9": {
        "summary": "Ensure Azure Bastion is used for VM access",
        "steps": [
            "1. Navigate to the NETWORK console section",
            "2. Review the configuration as described in: Ensure Azure Bastion is used for VM access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az network nsg list --query \"[].{Name:name,Location:location}\"",
    },
    "CIS-Azure-PG-10": {
        "summary": "Ensure PostgreSQL uses latest supported version",
        "steps": [
            "1. Navigate to the POSTGRESQL console section",
            "2. Review the configuration as described in: Ensure PostgreSQL uses latest supported version",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az postgres server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-PG-11": {
        "summary": "Ensure PostgreSQL backup retention is adequate",
        "steps": [
            "1. Navigate to the POSTGRESQL console section",
            "2. Review the configuration as described in: Ensure PostgreSQL backup retention is adequate",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az postgres server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-PG-12": {
        "summary": "Ensure PostgreSQL uses CMK encryption",
        "steps": [
            "1. Navigate to the POSTGRESQL console section",
            "2. Review the configuration as described in: Ensure PostgreSQL uses CMK encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az postgres server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-PG-13": {
        "summary": "Ensure PostgreSQL connection_throttling is on",
        "steps": [
            "1. Navigate to the POSTGRESQL console section",
            "2. Review the configuration as described in: Ensure PostgreSQL connection_throttling is on",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az postgres server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-PG-14": {
        "summary": "Ensure PostgreSQL log_duration is on",
        "steps": [
            "1. Navigate to the POSTGRESQL console section",
            "2. Review the configuration as described in: Ensure PostgreSQL log_duration is on",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az postgres server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-PG-7": {
        "summary": "Ensure PostgreSQL firewall does not allow 0.0.0.0/0",
        "steps": [
            "1. Navigate to the POSTGRESQL console section",
            "2. Review the configuration as described in: Ensure PostgreSQL firewall does not allow 0.0.0.0/0",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az postgres server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-PG-8": {
        "summary": "Ensure PostgreSQL server uses private endpoint",
        "steps": [
            "1. Navigate to the POSTGRESQL console section",
            "2. Review the configuration as described in: Ensure PostgreSQL server uses private endpoint",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az postgres server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-PG-9": {
        "summary": "Ensure PostgreSQL audit logging is enabled",
        "steps": [
            "1. Navigate to the POSTGRESQL console section",
            "2. Review the configuration as described in: Ensure PostgreSQL audit logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az postgres server list --query \"[].{Name:name,Version:version}\"",
    },
    "CIS-Azure-SQLDB-1": {
        "summary": "Ensure SQL Database zone redundancy is enabled",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database zone redundancy is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-SQLDB-2": {
        "summary": "Ensure SQL Database geo-replication is configured",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database geo-replication is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-SQLDB-3": {
        "summary": "Ensure SQL Database auto-failover groups are used",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database auto-failover groups are used",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-SQLDB-4": {
        "summary": "Ensure SQL Database dynamic data masking is configured",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database dynamic data masking is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-SQLDB-5": {
        "summary": "Ensure SQL Database uses private endpoint",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database uses private endpoint",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-SQLDB-6": {
        "summary": "Ensure SQL Database minimum TLS is 1.2",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database minimum TLS is 1.2",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-SQLDB-7": {
        "summary": "Ensure SQL Database ledger is enabled for audit trail",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database ledger is enabled for audit trail",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-SQLDB-8": {
        "summary": "Ensure SQL Database automatic tuning is enabled",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database automatic tuning is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-Azure-SQLDB-9": {
        "summary": "Ensure SQL Database has short-term backup retention >= 7 days",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure SQL Database has short-term backup retention >= 7 days",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/microsoft_azure",
        "cli_example": "az sql server list --query \"[].{Name:name,State:state}\"",
    },
    "CIS-GCP-1.11": {
        "summary": "Ensure API keys are not created for project",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure API keys are not created for project",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-1.11-KMS": {
        "summary": "Ensure KMS crypto keys have proper IAM bindings",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS crypto keys have proper IAM bindings",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud kms keys list --location=<location> --keyring=<keyring>",
    },
    "CIS-GCP-1.17": {
        "summary": "Ensure domain-restricted sharing is configured in org policy",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure domain-restricted sharing is configured in org policy",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-1.18": {
        "summary": "Ensure uniform bucket-level access is enabled in org policy",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure uniform bucket-level access is enabled in org policy",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-1.6": {
        "summary": "Ensure SA keys are managed and rotated",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SA keys are managed and rotated",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-1.7": {
        "summary": "Ensure user-managed SA keys are not created for default SAs",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure user-managed SA keys are not created for default SAs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-2.10": {
        "summary": "Ensure log metric filter exists for SQL config changes",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure log metric filter exists for SQL config changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.11": {
        "summary": "Ensure alert policies exist for all log metric filters",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure alert policies exist for all log metric filters",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.3": {
        "summary": "Ensure log metric filter exists for project ownership changes",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure log metric filter exists for project ownership changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.3-SINK": {
        "summary": "Ensure log exports are configured for all log entries",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure log exports are configured for all log entries",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.4-MF": {
        "summary": "Ensure log metric filter exists for audit config changes",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure log metric filter exists for audit config changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.6-MF": {
        "summary": "Ensure log metric filter exists for VPC firewall rule changes",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure log metric filter exists for VPC firewall rule changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.7-MF": {
        "summary": "Ensure log metric filter exists for VPC route changes",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure log metric filter exists for VPC route changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.8-MF": {
        "summary": "Ensure log metric filter exists for VPC network changes",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure log metric filter exists for VPC network changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.9-MF": {
        "summary": "Ensure log metric filter exists for storage IAM changes",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure log metric filter exists for storage IAM changes",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.LOG-ACC": {
        "summary": "Ensure logs access is restricted",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure logs access is restricted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.LOG-DATA": {
        "summary": "Ensure data access logs are enabled for all services",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure data access logs are enabled for all services",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.LOG-ENC": {
        "summary": "Ensure logging sinks use CMEK",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure logging sinks use CMEK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.LOG-RET": {
        "summary": "Ensure Cloud Audit Logs retention is configured",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure Cloud Audit Logs retention is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-2.LOG-VPC": {
        "summary": "Ensure VPC Flow Logs retention >= 30 days",
        "steps": [
            "1. Navigate to the LOGGING console section",
            "2. Review the configuration as described in: Ensure VPC Flow Logs retention >= 30 days",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud logging sinks list --format=\"table(name,destination)\"",
    },
    "CIS-GCP-3.10": {
        "summary": "Ensure firewall rules do not use default network",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure firewall rules do not use default network",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.11": {
        "summary": "Ensure VPC Network firewall rules are documented",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VPC Network firewall rules are documented",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.12": {
        "summary": "Ensure load balancers use SSL certificates",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure load balancers use SSL certificates",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.9": {
        "summary": "Ensure no HTTPS or SSL proxy allows weak cipher suites",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure no HTTPS or SSL proxy allows weak cipher suites",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.ARMOR": {
        "summary": "Ensure Cloud Armor security policies are applied",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Cloud Armor security policies are applied",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.CDN": {
        "summary": "Ensure Cloud CDN uses signed URLs/cookies",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Cloud CDN uses signed URLs/cookies",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.FW-LOG": {
        "summary": "Ensure firewall rules have logging enabled",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure firewall rules have logging enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.FW-PRI": {
        "summary": "Ensure firewall rules use priority ordering",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure firewall rules use priority ordering",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.FW-TAG": {
        "summary": "Ensure firewall rules use network tags or SAs for targeting",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure firewall rules use network tags or SAs for targeting",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.NAT": {
        "summary": "Ensure Cloud NAT is configured for private instances",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Cloud NAT is configured for private instances",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.PGA": {
        "summary": "Ensure Private Google Access is enabled for subnets",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Private Google Access is enabled for subnets",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.PSC": {
        "summary": "Ensure Private Service Connect is used for Google APIs",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Private Service Connect is used for Google APIs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-3.VPC-SC": {
        "summary": "Ensure VPC Service Controls perimeters are configured",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VPC Service Controls perimeters are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.12": {
        "summary": "Ensure Confidential Computing is enabled where required",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Confidential Computing is enabled where required",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.13": {
        "summary": "Ensure OS Config agent is installed and running",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure OS Config agent is installed and running",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.14": {
        "summary": "Ensure instance templates use secure boot",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure instance templates use secure boot",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.15": {
        "summary": "Ensure VM metadata does not contain SSH keys",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VM metadata does not contain SSH keys",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.16": {
        "summary": "Ensure sole-tenant nodes are used for sensitive workloads",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure sole-tenant nodes are used for sensitive workloads",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.17": {
        "summary": "Ensure preemptible VMs are not used for critical workloads",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure preemptible VMs are not used for critical workloads",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.18": {
        "summary": "Ensure managed instance groups use auto-healing",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure managed instance groups use auto-healing",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.19": {
        "summary": "Ensure instances have appropriate labels for management",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure instances have appropriate labels for management",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.2": {
        "summary": "Ensure instances do not use default SA with full API access",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure instances do not use default SA with full API access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.20": {
        "summary": "Ensure OS patch management is configured",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure OS patch management is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.21": {
        "summary": "Ensure VM access is controlled via OS Login with 2FA",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VM access is controlled via OS Login with 2FA",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.22": {
        "summary": "Ensure instances do not have external IPs unless required",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure instances do not have external IPs unless required",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.23": {
        "summary": "Ensure auto-delete is enabled for boot disks",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure auto-delete is enabled for boot disks",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.24": {
        "summary": "Ensure VM snapshot schedule is configured",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VM snapshot schedule is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.25": {
        "summary": "Ensure Integrity Monitoring is enabled for VMs",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Integrity Monitoring is enabled for VMs",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-4.7": {
        "summary": "Ensure VM disks are encrypted with CSEK or CMEK",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VM disks are encrypted with CSEK or CMEK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-5.10": {
        "summary": "Ensure Cloud Storage uses Autoclass for cost optimization",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage uses Autoclass for cost optimization",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-5.11": {
        "summary": "Ensure Cloud Storage objects use customer-managed encryption",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage objects use customer-managed encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-5.12": {
        "summary": "Ensure Cloud Storage does not allow allUsers/allAuthenticatedUsers",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage does not allow allUsers/allAuthenticatedUsers",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-5.4": {
        "summary": "Ensure Cloud Storage lifecycle rules are configured",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage lifecycle rules are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-5.5": {
        "summary": "Ensure Cloud Storage public access prevention is enforced",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage public access prevention is enforced",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-5.6": {
        "summary": "Ensure Cloud Storage uses CMEK encryption",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage uses CMEK encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-5.7": {
        "summary": "Ensure Cloud Storage retention policy is configured",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage retention policy is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-5.8": {
        "summary": "Ensure Cloud Storage access logging is enabled",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage access logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-5.9": {
        "summary": "Ensure Cloud Storage uses signed URLs for controlled access",
        "steps": [
            "1. Navigate to the STORAGE console section",
            "2. Review the configuration as described in: Ensure Cloud Storage uses signed URLs for controlled access",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gsutil ls -L gs://<bucket-name>/",
    },
    "CIS-GCP-6.1": {
        "summary": "Ensure Cloud SQL uses private IP only",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL uses private IP only",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.10": {
        "summary": "Ensure Cloud SQL replicas exist for critical databases",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL replicas exist for critical databases",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.11": {
        "summary": "Ensure Cloud SQL point-in-time recovery is enabled",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL point-in-time recovery is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.12": {
        "summary": "Ensure Cloud SQL password policy is configured",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL password policy is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.13": {
        "summary": "Ensure Cloud SQL deletion protection is enabled",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL deletion protection is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.14": {
        "summary": "Ensure Cloud SQL has appropriate labels",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL has appropriate labels",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.4": {
        "summary": "Ensure Cloud SQL uses SSL for all connections",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL uses SSL for all connections",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.5": {
        "summary": "Ensure Cloud SQL flags are configured securely",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL flags are configured securely",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.8": {
        "summary": "Ensure Cloud SQL maintenance window is configured",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL maintenance window is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-6.9": {
        "summary": "Ensure Cloud SQL uses CMEK encryption",
        "steps": [
            "1. Navigate to the SQL console section",
            "2. Review the configuration as described in: Ensure Cloud SQL uses CMEK encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud sql instances list --format=\"table(name,databaseVersion,state)\"",
    },
    "CIS-GCP-7.2": {
        "summary": "Ensure BigQuery datasets are not publicly accessible",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery datasets are not publicly accessible",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-7.3": {
        "summary": "Ensure BigQuery uses CMEK encryption",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery uses CMEK encryption",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-AUTH": {
        "summary": "Ensure BigQuery authorized views are used",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery authorized views are used",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-DLP": {
        "summary": "Ensure BigQuery uses DLP for sensitive data detection",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery uses DLP for sensitive data detection",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-EXPIRE": {
        "summary": "Ensure BigQuery table expiration is configured",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery table expiration is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-LABEL": {
        "summary": "Ensure BigQuery datasets are labeled",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery datasets are labeled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-LOG": {
        "summary": "Ensure BigQuery audit logging is enabled",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery audit logging is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-PART": {
        "summary": "Ensure BigQuery tables use partitioning",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery tables use partitioning",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-ROW": {
        "summary": "Ensure BigQuery row-level security policies exist",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery row-level security policies exist",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-SHIELD": {
        "summary": "Ensure BigQuery uses column-level security",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery uses column-level security",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-SLOT": {
        "summary": "Ensure BigQuery reservation slots are appropriate",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery reservation slots are appropriate",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-BQ-XREG": {
        "summary": "Ensure BigQuery cross-region backup is configured",
        "steps": [
            "1. Navigate to the BIGQUERY console section",
            "2. Review the configuration as described in: Ensure BigQuery cross-region backup is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "bq ls --format=prettyjson",
    },
    "CIS-GCP-DNS-1": {
        "summary": "Ensure Cloud DNS uses DNSSEC with strong algorithms",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure Cloud DNS uses DNSSEC with strong algorithms",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-10": {
        "summary": "Ensure DNS zones have description and purpose documented",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure DNS zones have description and purpose documented",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-11": {
        "summary": "Ensure DNS MX records use TLSA/DANE for email security",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure DNS MX records use TLSA/DANE for email security",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-12": {
        "summary": "Ensure DNS CAA records restrict certificate issuance",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure DNS CAA records restrict certificate issuance",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-13": {
        "summary": "Ensure wildcard DNS records are justified",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure wildcard DNS records are justified",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-2": {
        "summary": "Ensure Cloud DNS private zones are used for internal resolution",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure Cloud DNS private zones are used for internal resolution",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-3": {
        "summary": "Ensure Cloud DNS response policies are configured",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure Cloud DNS response policies are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-4": {
        "summary": "Ensure DNS zone delegation is properly configured",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure DNS zone delegation is properly configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-5": {
        "summary": "Ensure managed DNS zones are not publicly resolvable unnecessarily",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure managed DNS zones are not publicly resolvable unnecessarily",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-6": {
        "summary": "Ensure DNS logging is enabled for managed zones",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure DNS logging is enabled for managed zones",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-7": {
        "summary": "Ensure Cloud DNS peering is properly scoped",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure Cloud DNS peering is properly scoped",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-8": {
        "summary": "Ensure ALIAS/CNAME records do not point to external targets",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure ALIAS/CNAME records do not point to external targets",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-DNS-9": {
        "summary": "Ensure DNS zones have proper SOA TTL values",
        "steps": [
            "1. Navigate to the DNS console section",
            "2. Review the configuration as described in: Ensure DNS zones have proper SOA TTL values",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud dns managed-zones list --format=\"table(name,dnsName,visibility)\"",
    },
    "CIS-GCP-IAM-1": {
        "summary": "Ensure IAM recommender findings are reviewed",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure IAM recommender findings are reviewed",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-IAM-2": {
        "summary": "Ensure custom roles follow least privilege principle",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure custom roles follow least privilege principle",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-KMS-DEST": {
        "summary": "Ensure KMS key destruction is monitored",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS key destruction is monitored",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud kms keys list --location=<location> --keyring=<keyring>",
    },
    "CIS-GCP-KMS-HSM": {
        "summary": "Ensure KMS uses HSM protection level for sensitive keys",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS uses HSM protection level for sensitive keys",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud kms keys list --location=<location> --keyring=<keyring>",
    },
    "CIS-GCP-KMS-IMPORT": {
        "summary": "Ensure KMS key import jobs are audited",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS key import jobs are audited",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud kms keys list --location=<location> --keyring=<keyring>",
    },
    "CIS-GCP-KMS-LABEL": {
        "summary": "Ensure KMS keys are properly labeled",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS keys are properly labeled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud kms keys list --location=<location> --keyring=<keyring>",
    },
    "CIS-GCP-KMS-VER": {
        "summary": "Ensure KMS key versions are managed",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS key versions are managed",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud kms keys list --location=<location> --keyring=<keyring>",
    },
    "CIS-GCP-KMS-XPROJ": {
        "summary": "Ensure KMS keys are not shared cross-project without need",
        "steps": [
            "1. Navigate to the KMS console section",
            "2. Review the configuration as described in: Ensure KMS keys are not shared cross-project without need",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud kms keys list --location=<location> --keyring=<keyring>",
    },
    "CIS-GCP-NET-1": {
        "summary": "Ensure Cloud Interconnect links are encrypted",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Cloud Interconnect links are encrypted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-10": {
        "summary": "Ensure cross-project networking is controlled",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure cross-project networking is controlled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-2": {
        "summary": "Ensure VPN tunnels use IKEv2",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VPN tunnels use IKEv2",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-3": {
        "summary": "Ensure shared VPC is used for multi-project networking",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure shared VPC is used for multi-project networking",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-4": {
        "summary": "Ensure firewall insights are enabled",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure firewall insights are enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-5": {
        "summary": "Ensure internal load balancers use private IPs only",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure internal load balancers use private IPs only",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-6": {
        "summary": "Ensure Cloud DNS DNSSEC is enabled for public zones",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure Cloud DNS DNSSEC is enabled for public zones",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-7": {
        "summary": "Ensure VPC flow logs are enabled on all subnets",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure VPC flow logs are enabled on all subnets",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-8": {
        "summary": "Ensure IP aliases are used for secondary ranges",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure IP aliases are used for secondary ranges",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-NET-9": {
        "summary": "Ensure subnets use appropriate CIDR ranges",
        "steps": [
            "1. Navigate to the COMPUTE console section",
            "2. Review the configuration as described in: Ensure subnets use appropriate CIDR ranges",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud compute instances list --format=\"table(name,zone,status)\"",
    },
    "CIS-GCP-ORG-1": {
        "summary": "Ensure organization policy constraints are applied",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure organization policy constraints are applied",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-10": {
        "summary": "Ensure default network creation is disabled",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure default network creation is disabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-11": {
        "summary": "Ensure OS Login is required org-wide",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure OS Login is required org-wide",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-2": {
        "summary": "Ensure Security Command Center is enabled",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Security Command Center is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-3": {
        "summary": "Ensure org-level audit logging is configured",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure org-level audit logging is configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-4": {
        "summary": "Ensure Access Transparency is enabled",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Access Transparency is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-5": {
        "summary": "Ensure Access Approval is enabled for sensitive operations",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Access Approval is enabled for sensitive operations",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-6": {
        "summary": "Ensure assured workloads are configured for compliance",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure assured workloads are configured for compliance",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-7": {
        "summary": "Ensure resource location restriction is set",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure resource location restriction is set",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-8": {
        "summary": "Ensure VM external IP restriction is enforced",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure VM external IP restriction is enforced",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-ORG-9": {
        "summary": "Ensure serial port access is disabled org-wide",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure serial port access is disabled org-wide",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-1": {
        "summary": "Ensure SA keys are rotated within 90 days",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SA keys are rotated within 90 days",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-10": {
        "summary": "Ensure SA user role is restricted",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SA user role is restricted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-11": {
        "summary": "Ensure SA key usage is monitored",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SA key usage is monitored",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-2": {
        "summary": "Ensure SA keys have expiry set",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SA keys have expiry set",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-3": {
        "summary": "Ensure SAs do not have admin privileges",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SAs do not have admin privileges",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-4": {
        "summary": "Ensure SAs follow naming conventions",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SAs follow naming conventions",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-5": {
        "summary": "Ensure SAs are not used as actors in IAM bindings",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SAs are not used as actors in IAM bindings",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-6": {
        "summary": "Ensure disabled SAs have no active keys",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure disabled SAs have no active keys",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-7": {
        "summary": "Ensure SA keys are not embedded in application code",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SA keys are not embedded in application code",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-8": {
        "summary": "Ensure Workload Identity Federation is used instead of SA keys",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Workload Identity Federation is used instead of SA keys",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SA-9": {
        "summary": "Ensure SA token creator role is restricted",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure SA token creator role is restricted",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-1": {
        "summary": "Ensure unnecessary APIs are disabled",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure unnecessary APIs are disabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-10": {
        "summary": "Ensure Cloud Build workers use private pools",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Cloud Build workers use private pools",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-11": {
        "summary": "Ensure Cloud Build triggers use approved sources",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Cloud Build triggers use approved sources",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-12": {
        "summary": "Ensure Pub/Sub topics are encrypted with CMEK",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Pub/Sub topics are encrypted with CMEK",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-2": {
        "summary": "Ensure App Engine firewall rules are restrictive",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure App Engine firewall rules are restrictive",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-3": {
        "summary": "Ensure Cloud Functions use 2nd gen runtime",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Cloud Functions use 2nd gen runtime",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-4": {
        "summary": "Ensure Cloud Run services require authentication",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Cloud Run services require authentication",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-5": {
        "summary": "Ensure GKE clusters use Workload Identity",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure GKE clusters use Workload Identity",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-6": {
        "summary": "Ensure GKE Binary Authorization is enabled",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure GKE Binary Authorization is enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-7": {
        "summary": "Ensure GKE network policies are enabled",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure GKE network policies are enabled",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-8": {
        "summary": "Ensure Artifact Registry uses vulnerability scanning",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Artifact Registry uses vulnerability scanning",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
    "CIS-GCP-SVC-9": {
        "summary": "Ensure Cloud Armor WAF rules are configured",
        "steps": [
            "1. Navigate to the IAM console section",
            "2. Review the configuration as described in: Ensure Cloud Armor WAF rules are configured",
            "3. Apply the recommended settings per CIS benchmark guidance",
            "4. Verify the change is in effect",
        ],
        "documentation": "https://www.cisecurity.org/benchmark/google_cloud_computing_platform",
        "cli_example": "gcloud iam service-accounts list --format=\"table(email,disabled)\"",
    },
}
